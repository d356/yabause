machine:
  services:
    - docker

checkout:
  post:
    - cd .. && git clone https://github.com/d356/yabauseut-docker.git

test:
  override:
    - cd .. && ls && pwd && cp -r yabause yabauseut-docker && cd yabauseut-docker && ls && pwd && cd .. && cd yabauseut-docker && docker build -t yabauseut-tests .